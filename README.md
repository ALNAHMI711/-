# مشاهد

منصة شخصية لأتمتة صناعة المحتوى ونشره عبر القنوات الرسمية، مع وضع الرفع ووضع AI Autopilot.

## التشغيل المحلي

```bash
python -m pip install -r requirements.txt
python -m pytest -q
python -m app
```

لا تُحفظ مفاتيح API أو كلمات المرور أو رموز OAuth في المستودع.
