# Security Policy

## قواعد أساسية
- لا تضع مفاتيح API أو كلمات المرور أو رموز OAuth الحقيقية داخل Git.
- استخدم `.env` محلياً أو مدير أسرار في بيئة الإنتاج.
- لا تُسجّل الأسرار في logs أو رسائل الأخطاء.
- كلمات مرور المستخدمين تُخزّن باستخدام تجزئة قوية مثل Argon2id، وليس كنص صريح.
- جلسات الإدارة منفصلة ومحمية، مع تحديد المعدل وسجل تدقيق.
- نشر المحتوى لا يعمل افتراضياً قبل اكتمال الإعدادات المطلوبة.
- حسابات المنصات تُربط عبر OAuth والواجهات الرسمية؛ لا نطلب كلمات مرور المنصات.

## الإبلاغ عن مشكلة أمنية
لا تضع أسراراً أو بيانات حساسة في Issues العامة. استخدم قناة خاصة مناسبة للإبلاغ الأمني.
