[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mashahid"
version = "0.1.0"
description = "مشاهد - personal content automation platform"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["app*"]
